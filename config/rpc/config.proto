syntax = "proto3";
package rpc;

service Channel {
  rpc Publish(ConfigMsg)returns (ConfigMsg){}
  rpc Subscribe(ConfigMsg) returns (stream ConfigMsg) {}
}


message ConfigMsg {
  string Info = 1;
  string name = 2;
}
